# Changelog #8: 빈 방 유예 — 방치 시 로비로 튕기는 문제 해소

## 날짜
2026-07-29

## 변경 사유
테스트 피드백: 유저 1·2로 게임 중 입력 없이 방치했더니 방 만들기/참가 화면으로 돌아옴.

**원인 체인**:
1. 두 브라우저 모두 백그라운드 방치 → 탭 스로틀링/동결로 socket.io 하트비트 타임아웃 → 전원 연결 끊김
2. 서버가 접속자 0명이 된 방을 **즉시 삭제**
3. 탭이 깨어나 자동 재접속 → 방이 없음 → rejoin 실패 폴백으로 로비 초기화

추가 발견: 마지막 접속자가 끊기는 순간에도 `advanceTurn`이 돌아 **끊긴 전원을 0점
처리하고 게임을 스스로 종료**시키는 문제(유예를 넣어도 재접속 시 게임이 이미 끝나 있음).

## 수정: `server/src/app.js`

- **빈 방 유예**: 게임 중(playing/finished) 방은 전원이 끊겨도 즉시 삭제하지 않고
  `emptyRoomGraceMs`(기본 10분) 후 삭제. 그 사이 재접속하면 타이머 취소, 방 유지.
  대기실(waiting) 방은 기존대로 즉시 삭제. 타이머는 `unref()`로 프로세스 종료 비차단.
- **전원 이탈 시 게임 동결**: 접속자가 0명이 되면 턴을 넘기지 않고 상태를 그대로 보존.
- **재접속 시 재개**: 돌아온 시점에 현재 턴 주인이 아직 끊겨 있으면 그때 턴을 넘김
  (턴이 실제로 넘어간 경우에만 `turn:begin` 브로드캐스트 — 진행 중이던 턴은 건드리지 않음).
- **pingTimeout 20초 → 60초**: 탭 스로틀링 정도로는 연결이 안 끊기게.
- `createGameServer(options)`로 유예 시간 주입 가능 (테스트는 400ms로 검증).

## 검증
- 신규 테스트 2건: 유예 내 전원 이탈 → 재접속 시 방·게임 상태 유지 + 턴 인계 /
  유예 경과 → 방 삭제·재접속 불가
- 전체 테스트 12/12 통과
