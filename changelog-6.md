# Changelog #6: 3D 씬 개선 — 주사위 이탈 방지, 모서리 라운딩, 렌더링 최적화

## 날짜
2026-07-29

## 변경 사유
사용자 피드백 3건:

1. 주사위가 굴릴 때 판에서 굴러 떨어지는 경우가 있음
2. 주사위 모서리가 너무 각져 있어 둥글게 처리 원함
3. 프레임이 끊기는 느낌 — 최적화 필요

## 수정 파일: `client/src/components/three/GameScene.vue` (단일 파일)

### 1. 주사위 이탈 방지 — 물리 공간 완전 밀폐

**원인**: 벽이 y=3까지인데 주사위는 y=3~4에서 좌우 속도를 갖고 발사됨. 주사위끼리
공중 충돌하면 벽 위로 넘어갈 수 있고, 벽 두께 0.2는 고속 관통 여지도 있었음.

- 벽 높이 y -1~3 → **-1~7** (발사 최고점보다 높게)
- 벽 반두께 0.1 → **0.5**
- **천장 추가** (y=7.25) — 기하학적으로 탈출 불가능한 밀폐 상자
- 기존 낙하 복구 로직(`recoverFallenDice`)은 최후 안전장치로 유지

**검증**: cannon-es 헤드리스 시뮬레이션 300회 (30~144fps 가변 프레임 재현) —
구버전 벽 **탈출 64건(21%)** → 신버전 **0건**.

### 2. 주사위 모서리 라운딩

- `BoxGeometry` → three.js 공식 `RoundedBoxGeometry` (세그먼트 4, 반경 = 크기의 12%)
- RoundedBoxGeometry는 BoxGeometry를 상속하므로 **6면 재질 그룹이 유지됨**
  (헤드리스 검증: groups=6, materialIndex 0~5, UV 0~1 정상)
- 물리 콜라이더는 기존 Box 유지 (외형만 변경)

### 3. 렌더링 최적화

- **물리 시간 수정**: `world.step(1/120, 1/60, 5)`의 하드코딩된 1/60 → 실제 프레임
  경과 시간(0.1s 클램프). 120Hz 모니터에서 물리 2배속, 저사양에서 슬로모션 되던 문제 해결.
- **보간 동기화**: 메시를 `body.position` 대신 `interpolatedPosition/Quaternion`으로
  동기화 — substep 경계에서 발생하던 미세 떨림 제거. 정지 판정 시엔 실제 바디 자세로 스냅.
- **GC 끊김 제거**: `updateKeptVisuals`가 매 프레임 `new THREE.Color()` 30개를 할당하던
  것을 모듈 상수 재사용(`emissive.copy`)으로 변경.
- **유휴 시 생략**: 움직이는 주사위가 없으면 물리 스텝 생략. 그림자 맵(2048 PCFSoft)은
  `shadowMap.autoUpdate = false`로 두고 움직임이 있을 때 + 500ms 유예 동안만 갱신.
- 주사위 바디를 초기 STATIC으로 생성 (마운트 직후 화면 밖에서 무의미하게 낙하하던 것 제거).

## 검증
- vue-tsc: 신규 코드 타입 클린 (기존 Solver.iterations 이슈 1건만 잔존)
- vite build 성공
- 헤드리스 물리/지오메트리 검증 통과 (위 수치 참조)
- 시각 확인(라운딩 정도·눈금 표시)은 실제 브라우저에서 확인 필요
